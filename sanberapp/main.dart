double sum(double a, double b) {
  return a + b;
}

double sqare(double x) => x * x;

void main() {
  for (var i = 2; i <= 10; i++) {
    print(i);
  }
}

// printMessage(String message, String from, [String to = 'unknow']) {
//   print('"$message", from ${from} to ${to}');
// }
